# ⚠️ INSTALL INTO .venv ONLY — never into global python:
#   Windows:  python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt
#   Linux/mac: ./setup.sh   (creates .venv automatically)
# SpotDL pins an OLD stack (fastapi 0.103 / anyio 3.x) that BREAKS other
# global tools (openai, google-genai, httpx need anyio>=4.10).
# Pinned via `python -m pip index versions <pkg>` on 2026-09-09 (Python 3.14).
# Reproducible installs: `pip install -r requirements.txt`
# To upgrade: bump a pin, run `make test`, then commit.
flask==3.1.3
yt-dlp==2026.8.19
spotdl==4.5.2
spotapi==1.2.8
spotipyfree==1.9.14
pymongo==4.18.0
ytmusicapi==1.12.2
syncedlyrics==1.0.1
# Static ffmpeg binary per platform (no system ffmpeg needed on clean OS)
imageio-ffmpeg==0.6.0

# Build and packaging dependencies
pyinstaller==6.22.2

# Test runner (dev only, excluded from PyInstaller bundle via backend.spec excludes)
pytest==9.1.1
